# sqlc is no longer used in this project.
#
# sqlc does not support SQL Server / Azure SQL. The queries in
# internal/pkg/database/queries/ are written in T-SQL and will be executed
# directly via database/sql + github.com/microsoft/go-mssqldb in the
# Azure Functions database layer.
#
# The generated code in internal/pkg/database/db/ is kept as-is until the
# local service DB dependency is removed during the wiring step.
